# ncode-paper-app (private)

Custom app to overlay NeoSmartpen Ncode pattern onto a design PDF (MuPDF-based).
Place your pattern PDFs in a local folder named **Ncode paper** next to the EXE (not tracked).
Build: open `mupdfSample.sln` → x64|Release → Build → run `mupdfSample.exe input.pdf`.
